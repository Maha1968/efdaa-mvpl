export default function Home() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center px-6 py-16">
      <div className="w-full max-w-md text-center">
        <p className="text-sm font-medium uppercase tracking-widest text-emerald-700">
          EFDAA
        </p>
        <h1 className="mt-3 text-3xl font-semibold tracking-tight text-zinc-900">
          Referral attribution, offline
        </h1>
        <p className="mt-4 text-base leading-relaxed text-zinc-600">
          Share tokens via WhatsApp, trace who influenced each purchase, and
          split rewards across the chain.
        </p>
        <p className="mt-8 rounded-xl border border-zinc-200 bg-white px-4 py-3 text-sm text-zinc-500">
          Scaffold ready — features coming in the next steps.
        </p>
      </div>
    </main>
  );
}
